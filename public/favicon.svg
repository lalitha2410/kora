<!-- fill must match src/config/brand.ts's koraBrand.colors.accentDark — a
     static asset loaded directly by the browser, so it can't import the
     brand config; keep these two in sync by hand if accentDark changes. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="7" fill="#7A2A38"/>
  <text x="16" y="22" font-family="Georgia, serif" font-size="17" fill="#FFFFFF" text-anchor="middle">K</text>
</svg>
